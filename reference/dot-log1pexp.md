# Compute log(1 + exp(x)) with overflow/underflow protection

Compute log(1 + exp(x)) with overflow/underflow protection

## Usage

``` r
.log1pexp(x)
```

## Arguments

- x:

  Numeric vector.

## Value

Numeric vector: log(1 + exp(x)).
