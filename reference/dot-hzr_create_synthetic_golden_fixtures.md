# Generate and save golden fixtures for parity testing

Creates synthetic datasets and fits Weibull models using the R
implementation, storing results as golden fixtures for validation.

## Usage

``` r
.hzr_create_synthetic_golden_fixtures(output_dir = NULL)
```
