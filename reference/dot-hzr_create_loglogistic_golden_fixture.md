# Create and save log-logistic golden fixture

Generates a synthetic dataset and fits a log-logistic model, storing
results as a golden fixture for validation.

## Usage

``` r
.hzr_create_loglogistic_golden_fixture(output_dir = NULL)
```
