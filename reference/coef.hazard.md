# Extract coefficients from hazard model

Extract coefficients from hazard model

## Usage

``` r
# S3 method for class 'hazard'
coef(object, ...)
```

## Arguments

- object:

  A `hazard` object.

- ...:

  Unused; for S3 compatibility.
