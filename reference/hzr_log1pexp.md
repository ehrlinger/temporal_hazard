# Numerically stable log(1 + exp(x))

Numerically stable log(1 + exp(x))

## Usage

``` r
hzr_log1pexp(x)
```

## Arguments

- x:

  Numeric vector.

## Value

Numeric vector with element-wise `log(1 + exp(x))`.
