% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition.R
\name{hzr_phase_hazard}
\alias{hzr_phase_hazard}
\title{Instantaneous hazard contribution from a single phase}
\usage{
hzr_phase_hazard(
  time,
  t_half = 1,
  nu = 1,
  m = 0,
  type = c("cdf", "hazard", "constant")
)
}
\arguments{
\item{time}{Numeric vector of times (> 0).}

\item{t_half}{Half-life parameter (> 0).}

\item{nu}{Time exponent.}

\item{m}{Shape parameter.}

\item{type}{Phase type: \code{"cdf"} (early -- uses \eqn{G(t)}),
\code{"hazard"} (late -- uses cumulative hazard from \eqn{h(t)}), or
\code{"constant"} (flat rate -- \eqn{\Phi = t}).}
}
\value{
Numeric vector of instantaneous hazard contributions \eqn{\phi(t)},
same length as \code{time}.
}
\description{
Computes \eqn{\phi_j(t) = d\Phi_j/dt} for one phase -- the derivative of
the cumulative hazard contribution returned by \code{\link[=hzr_phase_cumhaz]{hzr_phase_cumhaz()}}.
}
\details{
\itemize{
\item \code{"cdf"}: \eqn{\phi(t) = g(t)} (density).
\item \code{"hazard"}: \eqn{\phi(t) = h(t) = g(t)/(1-G(t))}.
\item \code{"constant"}: \eqn{\phi(t) = 1}.
}
}
\examples{
t_grid <- seq(0.1, 10, by = 0.1)
phi_early <- hzr_phase_hazard(t_grid, t_half = 2, nu = 2, m = 0,
                               type = "cdf")
phi_late  <- hzr_phase_hazard(t_grid, t_half = 5, nu = 1, m = 0,
                               type = "hazard")

}
\seealso{
\code{\link[=hzr_decompos]{hzr_decompos()}} for the underlying parametric family,
\code{\link[=hzr_phase_cumhaz]{hzr_phase_cumhaz()}} for the cumulative version.
}
