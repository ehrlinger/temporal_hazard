% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood-multiphase.R
\name{.hzr_start_perturbations}
\alias{.hzr_start_perturbations}
\title{Deterministic multi-start perturbations}
\usage{
.hzr_start_perturbations(n_starts, n_par, seed, sd = 0.5)
}
\arguments{
\item{n_starts}{Number of optimization starts.}

\item{n_par}{Length of the free-parameter vector being perturbed.}

\item{seed}{Single whole number selecting the ensemble of offsets.}

\item{sd}{Standard deviation of the offsets (default 0.5).}
}
\value{
A list of \code{max(0, n_starts - 1)} numeric vectors of length \code{n_par};
empty for a single start, which is never perturbed.
}
\description{
Draws the offsets that shift the starting values for every multiphase start
after the first.  The draw is taken from an internally seeded stream and the
ambient \code{.Random.seed} is restored on exit, so a fit is reproducible without
\code{set.seed()} and never advances the caller's stream.  Both halves matter:
seeding alone would still let an intervening draw change the fit, and
restoring alone would leave the fit dependent on whatever the stream
happened to hold when it was called.
}
\keyword{internal}
