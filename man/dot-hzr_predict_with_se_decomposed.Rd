% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-cl.R
\name{.hzr_predict_with_se_decomposed}
\alias{.hzr_predict_with_se_decomposed}
\title{Per-phase + total cumulative-hazard predictions with delta-method CLs}
\usage{
.hzr_predict_with_se_decomposed(
  object,
  time,
  x_list,
  cov_counts,
  phases,
  level = 0.95
)
}
\arguments{
\item{object}{Fitted multiphase \code{hazard} object.}

\item{time}{Numeric prediction times (length n).}

\item{x_list}{Named list of per-phase design matrices.}

\item{cov_counts}{Named integer covariate counts.}

\item{phases}{Named list of \code{hzr_phase} objects.}

\item{level}{Confidence level.}
}
\value{
Long \code{data.frame(time, component, fit, se.fit, lower, upper)};
\code{component} is an ordered factor with levels \code{c("total", names(phases))}.
}
\description{
Long-format companion to \code{\link[=.hzr_predict_with_se]{.hzr_predict_with_se()}} for multiphase models under
\code{decompose = TRUE}. Computes log-scale CLs for the total cumulative hazard
and for each phase's additive contribution \verb{H_j(t) = mu_j(x) Phi_j(t)}.
Per-phase CLs use only that phase's parameter block, so they do NOT sum to
the total CL (cross-phase covariance contributes only to the total).
}
\keyword{internal}
