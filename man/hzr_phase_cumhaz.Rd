% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition.R
\name{hzr_phase_cumhaz}
\alias{hzr_phase_cumhaz}
\title{Cumulative hazard contribution from a single phase}
\usage{
hzr_phase_cumhaz(
  time,
  t_half = 1,
  nu = 1,
  m = 0,
  type = c("cdf", "hazard", "constant")
)
}
\arguments{
\item{time}{Numeric vector of times (> 0).}

\item{t_half}{Half-life parameter (> 0).}

\item{nu}{Time exponent.}

\item{m}{Shape parameter.}

\item{type}{Phase type: \code{"cdf"} (early -- uses \eqn{G(t)}),
\code{"hazard"} (late -- uses cumulative hazard from \eqn{h(t)}), or
\code{"constant"} (flat rate -- \eqn{\Phi = t}).}
}
\value{
Numeric vector of cumulative hazard contributions \eqn{\Phi(t)},
same length as \code{time}.
}
\description{
Computes \eqn{\Phi_j(t)} for one phase in the additive model
\eqn{H(t|x) = \sum_j \mu_j(x) \Phi_j(t)}.
}
\details{
\itemize{
\item \code{"cdf"}: \eqn{\Phi(t) = G(t)}.  Bounded \eqn{[0, 1]}.  Models early
risk that resolves over time.
\item \code{"hazard"}: \eqn{\Phi(t) = -\log(1 - G(t))}.  Monotone increasing.
Models late or aging risk.  This is the cumulative hazard derived from
the hazard function \eqn{h(t)}, since
\eqn{\int_0^t h(s)\,ds = -\log(1 - G(t))}.
\item \code{"constant"}: \eqn{\Phi(t) = t}.  Ignores \code{t_half}, \code{nu}, \code{m}.
Equivalent to exponential (constant hazard rate).
}
}
\examples{
t_grid <- seq(0.1, 10, by = 0.1)
phi_early <- hzr_phase_cumhaz(t_grid, t_half = 2, nu = 2, m = 0,
                               type = "cdf")
phi_late  <- hzr_phase_cumhaz(t_grid, t_half = 5, nu = 1, m = 0,
                               type = "hazard")
phi_const <- hzr_phase_cumhaz(t_grid, type = "constant")

}
\seealso{
\code{\link[=hzr_decompos]{hzr_decompos()}} for the underlying parametric family,
\code{\link[=hzr_phase_hazard]{hzr_phase_hazard()}} for the instantaneous hazard contribution.
}
