% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase-spec.R
\name{hzr_phase}
\alias{hzr_phase}
\title{Specify a single hazard phase}
\usage{
hzr_phase(
  type = c("cdf", "hazard", "constant"),
  t_half = 1,
  nu = 1,
  m = 0,
  formula = NULL
)
}
\arguments{
\item{type}{Character; phase type: \code{"cdf"}, \code{"hazard"}, or \code{"constant"}.}

\item{t_half}{Positive scalar; initial half-life (time at which
\eqn{G(t_{1/2}) = 0.5}).  Ignored for \code{"constant"} phases.
SAS early: \code{THALF}/\code{RHO}.  SAS late: relates to \code{TAU}.}

\item{nu}{Numeric scalar; initial time exponent.  Ignored for \code{"constant"}.
SAS early: \code{NU}.  SAS late: relates to \code{GAMMA}/\code{ETA}.}

\item{m}{Numeric scalar; initial shape exponent.  Ignored for \code{"constant"}.
SAS early: \code{M}.  SAS late: relates to \code{GAMMA}/\code{ALPHA}.}

\item{formula}{Optional one-sided formula (e.g. \code{~ age + nyha}) for
phase-specific covariates.  When \code{NULL} (default), the phase inherits
the global formula from \code{\link[=hazard]{hazard()}}.}
}
\value{
An S3 object of class \code{"hzr_phase"} with elements:
\describe{
\item{type}{Phase type string.}
\item{t_half}{Initial half-life.}
\item{nu}{Initial time exponent.}
\item{m}{Initial shape exponent.}
\item{formula}{Phase-specific formula or \code{NULL}.}
}
}
\description{
Creates an \code{hzr_phase} object describing one term in a multiphase additive
cumulative hazard model.  Pass a list of these to the \code{phases} argument of
\code{\link[=hazard]{hazard()}} when \code{dist = "multiphase"}.
}
\section{Phase types}{


\describe{
\item{\code{"cdf"}}{Early risk that resolves over time.
\eqn{\Phi(t) = G(t)}, bounded \eqn{[0, 1]}.
SAS equivalent: Early / G1 phase.}
\item{\code{"hazard"}}{Late or aging risk that accumulates.
\eqn{\Phi(t) = -\log(1 - G(t))}, monotone increasing.
SAS equivalent: Late / G3 phase.}
\item{\code{"constant"}}{Flat background hazard rate.
\eqn{\Phi(t) = t}. No shape parameters are estimated.
SAS equivalent: Constant / G2 phase.}
}
}

\examples{
# Classic 3-phase HAZARD pattern
early <- hzr_phase("cdf",      t_half = 0.5, nu = 2, m = 0)
const <- hzr_phase("constant")
late  <- hzr_phase("hazard",   t_half = 5,   nu = 1, m = 0)

# Phase with specific covariates
early_cov <- hzr_phase("cdf", t_half = 0.5, nu = 2, m = 0,
                        formula = ~ age + shock)

# Use in hazard():
# hazard(Surv(time, status) ~ age, data = dat,
#        dist = "multiphase",
#        phases = list(early = early, constant = const, late = late))

}
\seealso{
\code{\link[=hzr_decompos]{hzr_decompos()}} for the underlying parametric family,
\code{\link[=hzr_phase_cumhaz]{hzr_phase_cumhaz()}} and \code{\link[=hzr_phase_hazard]{hzr_phase_hazard()}} for computing
\eqn{\Phi(t)} and \eqn{\phi(t)} from these specifications.
}
